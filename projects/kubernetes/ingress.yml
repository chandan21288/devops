apiversion: networking.k8s.io/v1

kind: Ingress

metadata:
  name: nginx-ingress

spec:
  ingressClassName: nginx

  rules:
  - http:
      paths:
        - path: /
          path_type: Prifix

          backend:
            service:
              name: my-nginx
              port:
                number: 80
