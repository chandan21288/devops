---
- name: Install docker
  dnf:
    name: docker
    state: present

- name: Start docker
  service:
    name: docker
    state: started
    enabled: yes

- name: add ec2-user to docker group
  user:
    name: ec2-user
    groups: docker
    append: yes

